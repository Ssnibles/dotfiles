# FZF Rosé Pine Dawn Theme Colors
set -Ux FZF_COLOR_BG            "#faf4ed" # Base
set -Ux FZF_COLOR_BG_PLUS       "#faf4ed" # Surface
set -Ux FZF_COLOR_FG            "#575279" # Text
set -Ux FZF_COLOR_FG_PLUS       "#575279" # Text (often same as FG for standard text)
set -Ux FZF_COLOR_HL            "#eb6f92" # Love (matched characters highlight)
set -Ux FZF_COLOR_HL_PLUS       "#f6c177" # Gold (selected item's matched characters highlight)
set -Ux FZF_COLOR_HEADER        "#9ccfd8" # Foam (header text)
set -Ux FZF_COLOR_INFO          "#c4a7e7" # Iris (info text, like counts)
set -Ux FZF_COLOR_POINTER       "#ebbcba" # Rose (pointer symbol)
set -Ux FZF_COLOR_MARKER        "#f6c177" # Gold (marker for multi-select)
set -Ux FZF_COLOR_PROMPT        "#9ccfd8" # Foam (prompt text)
set -Ux FZF_COLOR_SPINNER       "#f6c177" # Gold (spinner animation)
set -Ux FZF_COLOR_BORDER        "#9893a5" # Muted (border color)
